# assets

Drop your hero photo here as `gavin.jpg` (roughly 4:5 portrait, ~880x1100px looks good on
retina). Until it exists the hero shows a dashed placeholder box instead of a broken image.

To use a different filename or format, update the `<img src>` in `index.html` (`#heroPhoto`).
